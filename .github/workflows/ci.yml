name: Portfolio CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Build Portfolio
        run: echo "Building Portfolio Website"

      - name: Deploy
        run: echo "Deploying Portfolio Website"
